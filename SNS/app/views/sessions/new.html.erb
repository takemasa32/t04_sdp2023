<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>ログイン</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Tailwind CSS -->
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <style>
    body {
      background: White;
    }
    h1 {
      font-size: 2rem; /* 見出しの文字サイズを調整 */
      text-align: center; /* 見出しを中央揃えに */
      margin-bottom: 20px; /* 下部に余白を追加 */
    }
    form {
      max-width: 400px; /* フォームの最大幅を指定 */
      margin: 0 auto; /* フォームを中央揃えに */
    }
    label {
      font-size: 1rem; /* ラベルの文字サイズを調整 */
      margin-bottom: 5px; /* ラベルの下部に余白を追加 */
      display: block; /* ラベルをブロック要素に変更 */
    }
    input {
      width: 100%; /* 入力フィールドを親要素いっぱいに広げる */
      padding: 8px; /* 入力フィールドの内側の余白を調整 */
      margin-bottom: 10px; /* 入力フィールドの下部に余白を追加 */
    }
    .btn {
      width: 100%; /* ボタンを広げる */
    }
    .signup-link {
      text-align: center; /* リンクを中央揃えに */
      margin-top: 10px; /* 上部に余白を追加 */
    }
  </style>
</head>

<body>
<section class="bg-gray-50 dark:bg-gray-900">
  <div class="flex flex-col items-center justify-center px-6 py-8 mx-auto md:h-screen lg:py-0">
      <a href="#" class="flex items-center mb-6 text-2xl font-semibold text-gray-900 dark:text-white">
          <%= image_tag("Logo.png", alt: "Sponser Hubのロゴ", class: "w-8 h-8 mr-2") %>
          Sp Hub
      </a>
      <div class="w-full bg-white rounded-lg shadow dark:border md:mt-0 sm:max-w-md xl:p-0 dark:bg-gray-800 dark:border-gray-700">
          <div class="p-6 space-y-4 md:space-y-6 sm:p-8">
              <h1 class="text-xl font-bold leading-tight tracking-tight text-gray-900 md:text-2xl dark:text-white">
                  ログイン
              </h1>
              <%= form_with model: @session, local: true do |form| %>
                <%= flash[:danger] %>
                <div>
                    <%= form.label :email, 'メールアドレス', class: 'block mb-2 text-sm font-medium text-gray-900 dark:text-white' %>
                    <%= form.email_field :email, class: 'bg-gray-50 border border-gray-300 text-gray-900 sm:text-sm rounded-lg focus:ring-primary-600 focus:border-primary-600 block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500' %>
                </div>
                <div>
                    <%= form.label :password, 'パスワード', class: 'block mb-2 text-sm font-medium text-gray-900 dark:text-white' %>
                    <%= form.password_field :password, class: 'bg-gray-50 border border-gray-300 text-gray-900 sm:text-sm rounded-lg focus:ring-primary-600 focus:border-primary-600 block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500' %>
                </div>
                <div class="flex items-center justify-between">
                    <div class="flex items-start">
                    </div>
                </div>
                <%= form.submit 'ログイン', class: 'w-full text-white bg-primary hover:bg-primary focus:ring-4 focus:outline-none focus:ring-primary font-medium rounded-lg text-sm px-5 py-2.5 text-center dark:bg-primary dark:hover:bg-primary dark:focus:ring-primary' %>
                <p class="text-sm font-light text-gray-500 dark:text-gray-400">
                    アカウントがない方は<%= link_to "こちら", signup_path, class: 'font-medium text-primary-600 hover:underline dark:text-primary-500' %>へ
                </p>
              <% end %>
          </div>
      </div>
  </div>
</section>
<!-- Bootstrap JS (Optional) -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>



<!--
  <h1>ログイン</h1>

  <%= form_with model: @session, local: true do |form| %>
    <%= flash[:danger] %>

    <div class="mb-3">
      <%= form.label :email, 'メールアドレス' %>
      <%= form.email_field :email, class: 'form-control' %>
    </div>

    <div class="mb-3">
      <%= form.label :password, 'パスワード' %>
      <%= form.password_field :password, class: 'form-control' %>
    </div>
    
    <%= form.submit 'ログイン', class: 'btn btn-primary' %>

    <div class="signup-link">
      アカウントがない方は<%= link_to "こちら", signup_path, class: 'text-primary' %>へ
    </div>
  <% end %>

  <!-- Bootstrap JS (Optional) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>