<div class="mx-auto md:w-2/3 w-full flex">
  <div class="mx-auto">
    <% if notice.present? %>
      <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
    <% end %>

    <% if @profile.nil? %>
      <p>プロフィールが存在しません.</p>
      <p>プロフィールを <%= link_to "こちら", new_profile_path %>から作成してください．</p>
    <% else %>
      <%= render @profile %>

      <%= link_to "profileを編集する", edit_profile_path(@profile), class: "mt-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
      <%= link_to "投稿一覧へ", posts_path, class: "mt-2 rounded-lg py-3 px-5 bg-gray-100 font-medium" %>
      <div class="inline-block ml-2">
        <%= button_to "profileを削除する", profile_path(@profile), method: :delete, class: "mt-2 rounded-lg py-3 px-5 bg-gray-100 font-medium" %>
      </div>
    <% end %>
  </div>
</div>