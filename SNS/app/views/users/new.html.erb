<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Sacramento&display=swap');
    
    body {
      background: white;
      font-size: 12px;
      font-family: sans-serif; /* Use an appropriate font-family */
    }

    #registration-form {
      max-width: 250px;
      margin: 50px auto;
    }

    #registration-form h1 {
      text-align: center;
      font-family: 'Sacramento', cursive;
    }

    #registration-form label {
      display: block;
      margin: 15px 0;
    }

    #registration-form input {
      width: 100%;
      padding: 8px;
      box-sizing: border-box;
    }

    #registration-form p {
      margin-top: 0px;
      margin-bottom: 15px;
      font-size: 10px;
      color: #888;
    }

    #registration-form div.submit-button {
      text-align: center;
      font-size: 15px;
      margin-top: 60px;
      background: #1E90FF;
      color: white;
      border-radius: 5px;
      padding: 0px;
    }
    
    img{
      width: 80px;
      margin: auto;
      display: block;
    }
    
  </style>
</head>
<body>
  <div id="registration-form">
    
      <img src="assets/handshake.PNG" alt="握手のロゴ">
      
    <%= form_with model: @user, url: signup_path, local: true do |form| %>
      <% if @user.errors.any? %>
        <strong><%= @user.errors.count %>個のエラーがあります。</strong>
        <ul>
          <% @user.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      <% end %>

      <div>
        <%= form.label :email, 'メールアドレス' %>
        <%= form.email_field :email %>
      </div>

      <div>
        <%= form.label :password, 'パスワード' %>
        <%= form.password_field :password %>
        <p>※パスワードは6文字以上にしてください．</p>
      </div>

      <div>
        <%= form.label :password_confirmation, 'パスワード(確認)' %>
        <%= form.password_field :password_confirmation %>
      </div>

      <div>
        <%= form.label :name, 'ユーザー名' %>
        <%= form.text_field :name %>
      </div>

      <div class="submit-button">
        <%= form.submit '登録' %>
      </div>
    <% end %>
  </div>
</body>
</html>